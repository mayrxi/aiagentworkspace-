
# Prototype - Web

Open `index.html` in a browser to view the static prototype. It demonstrates:
- Left sidebar with agent list
- Collaboration feed (main panel)
- Create Agent modal to add new agents (client-side only)

# Prototype - API

A minimal Flask app `app.py` simulates agent creation and run endpoints.
Run:
    pip install flask
    python app.py
